import React, { useState } from 'react';

const Matches = ({ imageUrl, setImageUrl }) => {
  return (
    <div className="matches">
    <h1>your matches</h1>
    </div>
  );
};

export default Matches;